# This module has moved to src/shared/.
# Please run from repo root:
#   python -m src.shared.classifier_comparison
#
# If you see this file, please run:
#   git rm -r shared/
raise ImportError(
    "Package 'shared' has moved to src/shared/. "
    "Run 'python -m src.shared.classifier_comparison' from the repo root."
)
